# Core Configuration
OUTPUT_DIR=outputs
FINGERPRINTS_PATH=data/fingerprints.json
WEIGHTS_PATH=weights/detector.pt

# File Upload Limits
MAX_FILE_SIZE_MB=10
MAX_IMAGE_DIMENSION=4096
MIN_IMAGE_DIMENSION=224

# Detection Thresholds
FACE_CONFIDENCE_THRESHOLD=0.90
QUALITY_MIN_SIDE=224

# Model configuration (use MODEL_PROVIDER=huggingface or local_torch)
MODEL_PROVIDER=huggingface
MODEL_NAME=microsoft/deepfake-detection

# Logging
LOG_LEVEL=INFO